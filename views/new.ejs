<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>new chat</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-200 min-h-screen">
    <div class="min-h-screen flex flex-col justify-center items-center">
        <div>
            <a class="text-black pb-4" href="/profile">✖️</a>
        </div>
        <form class="w-full pt-4 max-w-sm" method="post" action="/chats">
            <div class="bg-black border-2 border-black flex flex-col p-6 gap-4 rounded-lg">
                <div>
                    <h1 class="text-white text-center font-bold">Create a New post</h1>
                    <label class="text-white font-medium">To</label>
                    <input
                        class="w-full mt-1 text-white bg-black border-2 border-white rounded-md italic px-2 py-1
                            focus:outline-none focus:ring-2 focus:ring-green-500"
                        type="text"
                        placeholder="this post is for"
                        name="to"
                        required
                    >
                </div>
                <div>
                    <label class="text-white font-medium">Post</label>
                    <textarea
                        class="w-full mt-1 text-white bg-black border-2 border-white rounded-md italic px-2 py-1
                            focus:outline-none focus:ring-2 focus:ring-green-500"
                        type="text"
                        placeholder="your gossip"
                        name="msg"
                        required
                    ></textarea>
                </div>

                <button
                    class="mt-2 rounded-md bg-white text-black border-2 py-1 cursor-pointer
                            hover:bg-gray-200 transition"
                    type="submit"
                    >
                    Post
                </button>
            </div>
        </form>
    </div>
</body>
</html>